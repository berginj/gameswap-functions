{
  "name": "gameswap-functions",
  "private": true,
  "workspaces": [
    "fsa-www"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix fsa-www run dev\" \"func start\"",
    "dev:ui": "npm --prefix fsa-www run dev",
    "dev:functions": "func start",
    "build": "npm --prefix fsa-www run build",
    "lint:ui": "npm --prefix fsa-www run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
